{
  "name": "streamcore-web-e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:live-auth": "playwright test --config playwright.live.config.ts",
    "test:player": "playwright test tests/player.spec.ts",
    "validate:release": "node validate-release-artifact.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  }
}
